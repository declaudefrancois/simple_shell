/**
 * main - Program to test the shell.
 * @ac: Arguments count.
 * @av: NULL terminated array of arguments string.
 * @env: NULL terminated array of environment variable.
 *
 * Return: Allways 0.
 */
int main(int ac, char **av, char **env)
{
	return (0);
}
